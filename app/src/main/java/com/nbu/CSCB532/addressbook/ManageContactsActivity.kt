package com.nbu.CSCB532.addressbook

import android.os.Bundle
import android.widget.Button
import androidx.appcompat.app.AppCompatActivity
import com.nbu.CSCB532.addressbook.R

class ManageContactsActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_manage_contacts)

/*        val addNewContactButton = findViewById<Button>(R.id.addNewContactButton)
        val viewContactsButton = findViewById<Button>(R.id.viewContactsButton)

        addNewContactButton.setOnClickListener {
            // Handle Add New Contact button click
        }

        viewContactsButton.setOnClickListener {
            // Handle View Contacts button click
        }*/
    }
}
